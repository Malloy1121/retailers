import { FixBoxDirective } from './fix-box.directive';

describe('FixBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new FixBoxDirective();
    expect(directive).toBeTruthy();
  });
});
